package practice_archive.beauty_of_programming.chapter_2;

/**
 * @author hiki on 2018-04-25
 */

public class MaxSubArraySum {

    /**
     * 求数组的子数组之和的最大值
     * 思路：动态规划，递推关系：MAX(i) = Math.max(MAX(i-1)+A[i],A[i]);
     * @param nums
     * @return
     */
    static int maxSubArray(int[] nums) {
        // todo
        return 0;
    }
}
